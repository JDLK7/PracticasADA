{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "Debug",
            "type": "gdb",
            "request": "launch",
            "target": "./caminante",
            "cwd": "${workspaceRoot}",
            "arguments": "-f 2.map"
        }
    ]
}